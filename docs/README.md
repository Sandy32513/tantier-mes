# Operon OPS 1.0 - GitHub Pages

This `docs/` site is the public landing page for the Operon OPS project.

## Full application
The full OPS system needs Node.js + Express + MySQL and runs locally or on a server.

```bash
cd "D:\adrive backup\SS-MES\tantier-mes"
npm install
npm run setup
npm start
```

Open: `http://localhost:3000`
